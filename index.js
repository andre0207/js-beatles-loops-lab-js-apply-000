// add solution here

const theBeatlesPlay = (musicians, instruments) => {
  let newArr = [];
  for (let i = 0; i < musicians.length; i++) {
    for (let i = 0; i < instruments.length; i++) {
      return newArr.push(`${musicians[i]} plays ${instruments[i]}`)
    }
  }
}